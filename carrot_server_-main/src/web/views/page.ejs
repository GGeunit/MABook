<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= data.title %></title>
</head>
<body>
  <h1><%= data.title %></h1>
  <p><%= data.content %></p>
  <% data.sections.forEach(section => { %>
    <h2><%= section.title %></h2>
    <p><%= section.content %></p>
  <% }) %>
</body>
</html>